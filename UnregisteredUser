class UnregisteredUser extends RegisteredUser {
    public UnregisteredUser(String name) {
        super(name, null, null); // Unregistered user without a password and registration ID
    }

    @Override
    public String toString() {
        return "UnregisteredUser [name=" + getName() + "]";
    }
}
